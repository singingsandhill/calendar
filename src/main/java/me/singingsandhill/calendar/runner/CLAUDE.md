# Runner Module

Running crew (97 Runners) attendance management. Domain: `Run (1) -- (*) Attendance`.

Admin dashboard uses Spring Security form login with `ROLE_ADMIN`. Default credentials configured via `runner.admin.username/password` properties.
